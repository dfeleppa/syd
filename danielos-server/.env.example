# Bind ONLY to the Mac mini's Tailscale IP
HOST=100.78.99.91
PORT=8787

# Postgres connection (we'll create user/db)
DATABASE_URL=postgres://danielos@localhost:5432/danielos

# Single-user API token (Bearer)
API_TOKEN=REPLACE_ME

# Optional: if you later build a web client
# CORS_ORIGIN=http://localhost:3000
